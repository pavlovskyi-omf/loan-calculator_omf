# Example environment variables for local development
# Copy this file to `.env` and replace the placeholder with your real key.
# WARNING: Values in `.env` are embedded in the client bundle by Vite and are
# visible to users. For production, prefer a server-side proxy or restrict the
# key permissions.

VITE_CURRENCYBEACON_KEY=your_currency_beacon_key_here
